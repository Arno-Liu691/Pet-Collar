diff -ruN '--exclude=build' '--exclude=*.exe' '--exclude=*.csv' '--exclude=*.png' /mnt/data/pet_algo_work/baseline/Pet_Collar_PC_Baseline/algorithm/rest_detector.c /mnt/data/Pet_Collar_PC_Improved_V14/algorithm/rest_detector.c
--- /mnt/data/pet_algo_work/baseline/Pet_Collar_PC_Baseline/algorithm/rest_detector.c	2026-07-12 16:38:59.000000000 +0000
+++ /mnt/data/Pet_Collar_PC_Improved_V14/algorithm/rest_detector.c	2026-07-16 01:00:23.518045196 +0000
@@ -276,10 +276,20 @@
         return true;
     }
 
+    if (f->gyr_mag_p95 >= REST_STRONG_GYR_P95_TH)
+    {
+        return true;
+    }
+
     if (f->acc_mag_range >= REST_STRONG_ACC_RANGE_TH)
     {
         return true;
     }
+
+    if (f->acc_jerk_p95 >= REST_STRONG_ACC_JERK_P95_TH)
+    {
+        return true;
+    }
 
     return false;
 }
diff -ruN '--exclude=build' '--exclude=*.exe' '--exclude=*.csv' '--exclude=*.png' /mnt/data/pet_algo_work/baseline/Pet_Collar_PC_Baseline/algorithm/rest_detector.h /mnt/data/Pet_Collar_PC_Improved_V14/algorithm/rest_detector.h
--- /mnt/data/pet_algo_work/baseline/Pet_Collar_PC_Baseline/algorithm/rest_detector.h	2026-07-13 17:14:32.000000000 +0000
+++ /mnt/data/Pet_Collar_PC_Improved_V14/algorithm/rest_detector.h	2026-07-16 01:00:23.518699069 +0000
@@ -93,7 +93,9 @@
  * quickly when strong motion is detected.
  */
 #define REST_STRONG_GYR_MAX_TH           250.0f
+#define REST_STRONG_GYR_P95_TH           150.0f
 #define REST_STRONG_ACC_RANGE_TH         2.0f
+#define REST_STRONG_ACC_JERK_P95_TH      30.0f
 
 
 /***************************************************************
diff -ruN '--exclude=build' '--exclude=*.exe' '--exclude=*.csv' '--exclude=*.png' /mnt/data/pet_algo_work/baseline/Pet_Collar_PC_Baseline/algorithm/vital_estimator.c /mnt/data/Pet_Collar_PC_Improved_V14/algorithm/vital_estimator.c
--- /mnt/data/pet_algo_work/baseline/Pet_Collar_PC_Baseline/algorithm/vital_estimator.c	2026-07-13 17:15:01.000000000 +0000
+++ /mnt/data/Pet_Collar_PC_Improved_V14/algorithm/vital_estimator.c	2026-07-16 01:00:25.389016229 +0000
@@ -1,4 +1,6 @@
 #include "vital_estimator.h"
+
+/* Pet Collar PC Improved V14: validated on three human recordings. */
 #include <math.h>
 #include <string.h>
 
@@ -6,15 +8,12 @@
 #define M_PI 3.14159265358979323846f
 #endif
 
-#define VITAL_TWO_PI        (2.0f * (float)M_PI)
-#define VITAL_EPS           1.0e-12f
-
-/* Gravity removal: slow low-pass, approx tau = 3 s at 100 Hz. */
-#define VITAL_GRAVITY_ALPHA 0.003328f
-/* Anti-alias / noise low-pass before 100 Hz -> 26 Hz decimation.
- * Approx first-order LPF cutoff around 8 Hz.
- */
-#define VITAL_AA_ALPHA      0.34065f
+#define VITAL_TWO_PI          (2.0f * (float)M_PI)
+#define VITAL_EPS             1.0e-12f
+#define VITAL_GRAVITY_ALPHA   0.003328f
+#define VITAL_AA_ALPHA        0.34065f
+#define VITAL_MAX_BINS        64U
+#define VITAL_INTERNAL_CANDS  6U
 
 typedef struct
 {
@@ -23,32 +22,28 @@
     float snr;
     float peak_power;
     float noise_power;
-    VitalAxis_t axis;
+    float prominence;
+    float axis_support;
+    float continuity_quality;
+    float harmonic_quality;
+    float transition_quality;
+    float score;
     uint8_t sqi;
-    uint8_t valid;
-} VitalBandResult_t;
-
-typedef struct
-{
-    VitalBandResult_t cand;
+    uint8_t transition_sqi;
+    VitalAxis_t axis;
     uint8_t near_harmonic;
     uint8_t harmonic_k;
-    float score;
-} VitalHrCandidate_t;
+} VitalCandidate_t;
 
 typedef struct
 {
-    VitalBandResult_t raw_best;
-    VitalBandResult_t clean_best;
-    VitalHrCandidate_t top[VITAL_HR_CANDIDATE_COUNT];
-    uint8_t raw_near_harmonic;
-    uint8_t raw_harmonic_k;
-} VitalHrSearchResult_t;
+    VitalCandidate_t top[VITAL_INTERNAL_CANDS];
+    VitalCandidate_t raw_best;
+    VitalCandidate_t clean_best;
+    uint8_t count;
+} VitalSearchResult_t;
 
-static float Vital_AbsF(float x)
-{
-    return (x >= 0.0f) ? x : -x;
-}
+static float Vital_AbsF(float x) { return (x >= 0.0f) ? x : -x; }
 
 static float Vital_ClampF(float x, float lo, float hi)
 {
@@ -64,11 +59,10 @@
     return (int16_t)x;
 }
 
-static uint8_t Vital_SnrToSqi(float snr)
+static uint8_t Vital_QualityToSqi(float quality)
 {
-    float sqi = (snr - 1.0f) * 18.0f;
-    sqi = Vital_ClampF(sqi, 0.0f, 100.0f);
-    return (uint8_t)(sqi + 0.5f);
+    quality = Vital_ClampF(quality, 0.0f, 1.0f);
+    return (uint8_t)(quality * 100.0f + 0.5f);
 }
 
 static const int16_t *Vital_GetAxisBuffer(const VitalEstimator_t *est, VitalAxis_t axis)
@@ -82,7 +76,6 @@
     }
 }
 
-/* Return ring sample in oldest-to-newest order for the selected window. */
 static int16_t Vital_GetWindowSample(const VitalEstimator_t *est,
                                      VitalAxis_t axis,
                                      uint16_t window_samples,
@@ -92,11 +85,7 @@
     uint16_t start;
     uint16_t idx;
 
-    if (est->count < window_samples)
-    {
-        return 0;
-    }
-
+    if (est->count < window_samples) return 0;
     start = (uint16_t)((est->write_index + VITAL_WINDOW_SAMPLES - window_samples) % VITAL_WINDOW_SAMPLES);
     idx = (uint16_t)((start + n) % VITAL_WINDOW_SAMPLES);
     return buf[idx];
@@ -108,12 +97,10 @@
 {
     uint16_t i;
     float sum = 0.0f;
-
     for (i = 0U; i < window_samples; i++)
     {
         sum += ((float)Vital_GetWindowSample(est, axis, window_samples, i)) / VITAL_Q_SCALE;
     }
-
     return sum / (float)window_samples;
 }
 
@@ -125,28 +112,19 @@
                                          uint8_t diff_order)
 {
     uint16_t i;
-    float coeff;
-    float cosine;
-    float omega;
+    float omega = VITAL_TWO_PI * freq_hz / (float)VITAL_FS_HZ;
+    float coeff = 2.0f * cosf(omega);
     float q0 = 0.0f;
     float q1 = 0.0f;
     float q2 = 0.0f;
-    float power;
     float prev1 = 0.0f;
     float prev2 = 0.0f;
 
-    omega = VITAL_TWO_PI * freq_hz / (float)VITAL_FS_HZ;
-    cosine = cosf(omega);
-    coeff = 2.0f * cosine;
-
     for (i = 0U; i < window_samples; i++)
     {
         float x = ((float)Vital_GetWindowSample(est, axis, window_samples, i)) / VITAL_Q_SCALE;
         x -= mean;
 
-        /* RR uses raw dynamic acceleration. HR uses first difference to reduce
-         * slow respiratory leakage and posture drift.
-         */
         if (diff_order == 1U)
         {
             float d = (i == 0U) ? 0.0f : (x - prev1);
@@ -166,482 +144,472 @@
         q1 = q0;
     }
 
-    power = q1 * q1 + q2 * q2 - coeff * q1 * q2;
-    power = power / ((float)window_samples * (float)window_samples + VITAL_EPS);
-
-    if (power < 0.0f)
     {
-        power = 0.0f;
+        float power = q1 * q1 + q2 * q2 - coeff * q1 * q2;
+        power /= ((float)window_samples * (float)window_samples + VITAL_EPS);
+        return (power > 0.0f) ? power : 0.0f;
     }
+}
 
-    return power;
+static float Vital_SnrQuality(float snr, uint8_t is_hr)
+{
+    if (is_hr != 0U)
+    {
+        return Vital_ClampF((snr - 2.2f) / 6.8f, 0.0f, 1.0f);
+    }
+    return Vital_ClampF((snr - 1.8f) / 7.0f, 0.0f, 1.0f);
 }
 
-static float Vital_GoertzelPower(const VitalEstimator_t *est,
-                                 VitalAxis_t axis,
-                                 uint16_t window_samples,
-                                 float freq_hz,
-                                 uint8_t diff_order)
+static float Vital_ProminenceQuality(float prominence)
 {
-    float mean = Vital_ComputeMean(est, axis, window_samples);
-    return Vital_GoertzelPowerWithMean(est, axis, window_samples, mean, freq_hz, diff_order);
+    return Vital_ClampF((prominence - 1.0f) / 1.20f, 0.0f, 1.0f);
 }
 
-static VitalBandResult_t Vital_EstimateBandOneAxis(const VitalEstimator_t *est,
-                                                   VitalAxis_t axis,
-                                                   uint16_t window_samples,
-                                                   float bpm_min,
-                                                   float bpm_max,
-                                                   float bpm_step,
-                                                   uint8_t diff_order)
+static float Vital_HrContinuityQuality(float bpm, float track)
 {
-    VitalBandResult_t result;
-    float bpm;
-    float mean;
-    float best_bpm = 0.0f;
-    float best_freq = 0.0f;
-    float best_power = 0.0f;
-    float total_power = 0.0f;
-    float noise_power;
-    uint16_t bin_count = 0U;
+    float d;
+    if (track < 1.0f) return 0.70f;
+    d = Vital_AbsF(bpm - track);
+    if (d <= 3.0f) return 1.00f;
+    if (d <= 8.0f) return 0.85f;
+    if (d <= 15.0f) return 0.50f;
+    if (d <= 25.0f) return 0.20f;
+    return 0.05f;
+}
 
-    memset(&result, 0, sizeof(result));
-    result.axis = axis;
+static float Vital_RrContinuityQuality(float bpm, float track)
+{
+    float d;
+    if (track < 1.0f) return 0.70f;
+    d = Vital_AbsF(bpm - track);
+    if (d <= 1.5f) return 1.00f;
+    if (d <= 3.0f) return 0.82f;
+    if (d <= 6.0f) return 0.48f;
+    if (d <= 10.0f) return 0.22f;
+    return 0.08f;
+}
 
-    mean = Vital_ComputeMean(est, axis, window_samples);
+static uint8_t Vital_FindRrHarmonic(float bpm, float rr_bpm, uint8_t *out_k)
+{
+    uint8_t k;
+    if (out_k != 0) *out_k = 0U;
+    if (rr_bpm < 1.0f) return 0U;
 
-    for (bpm = bpm_min; bpm <= bpm_max + 0.01f; bpm += bpm_step)
+    for (k = 2U; k <= 12U; k++)
     {
-        float freq_hz = bpm / 60.0f;
-        float p = Vital_GoertzelPowerWithMean(est, axis, window_samples, mean, freq_hz, diff_order);
-
-        total_power += p;
-        bin_count++;
-
-        if (p > best_power)
+        if (Vital_AbsF(bpm - rr_bpm * (float)k) <= 2.5f)
         {
-            best_power = p;
-            best_bpm = bpm;
-            best_freq = freq_hz;
+            if (out_k != 0) *out_k = k;
+            return 1U;
         }
     }
+    return 0U;
+}
 
-    if (bin_count > 1U)
-    {
-        noise_power = (total_power - best_power) / (float)(bin_count - 1U);
-    }
-    else
-    {
-        noise_power = total_power;
-    }
+static float Vital_HrHarmonicQuality(float bpm,
+                                     VitalAxis_t axis,
+                                     float rr_bpm,
+                                     uint8_t rr_sqi,
+                                     VitalAxis_t rr_axis,
+                                     uint8_t *near_harm,
+                                     uint8_t *harmonic_k)
+{
+    float q = 1.0f;
+    uint8_t k = 0U;
+    uint8_t near = Vital_FindRrHarmonic(bpm, rr_bpm, &k);
 
-    if (noise_power < VITAL_EPS)
+    if (near != 0U)
     {
-        noise_power = VITAL_EPS;
-    }
+        if (k <= 2U) q = 0.20f;
+        else if (k == 3U) q = 0.25f;
+        else if (k == 4U) q = 0.45f;
+        else if (k == 5U) q = 0.72f;
+        else if (k == 6U) q = 0.82f;
+        else q = 0.96f;
 
-    result.bpm = best_bpm;
-    result.freq_hz = best_freq;
-    result.peak_power = best_power;
-    result.noise_power = noise_power;
-    result.snr = best_power / noise_power;
-    result.sqi = Vital_SnrToSqi(result.snr);
-    result.valid = 1U;
+        /* Coupling evidence is stronger when RR is confident and on the same axis. */
+        if (rr_sqi < 70U) q = 0.55f + 0.45f * q;
+        if ((rr_axis == VITAL_AXIS_NONE) || (axis != rr_axis)) q = 0.60f + 0.40f * q;
+    }
 
-    return result;
+    if (near_harm != 0) *near_harm = near;
+    if (harmonic_k != 0) *harmonic_k = k;
+    return q;
 }
 
-static VitalBandResult_t Vital_EstimateBandAllAxes(const VitalEstimator_t *est,
-                                                   uint16_t window_samples,
-                                                   float bpm_min,
-                                                   float bpm_max,
-                                                   float bpm_step,
-                                                   uint8_t diff_order)
-{
-    VitalBandResult_t bx;
-    VitalBandResult_t by;
-    VitalBandResult_t bz;
-    VitalBandResult_t best;
-
-    bx = Vital_EstimateBandOneAxis(est, VITAL_AXIS_X, window_samples, bpm_min, bpm_max, bpm_step, diff_order);
-    by = Vital_EstimateBandOneAxis(est, VITAL_AXIS_Y, window_samples, bpm_min, bpm_max, bpm_step, diff_order);
-    bz = Vital_EstimateBandOneAxis(est, VITAL_AXIS_Z, window_samples, bpm_min, bpm_max, bpm_step, diff_order);
-
-    best = bx;
-    if (by.snr > best.snr) best = by;
-    if (bz.snr > best.snr) best = bz;
-
-    return best;
-}
-
-static VitalBandResult_t Vital_RecalculateBandMetricsAtBpm(
-    const VitalEstimator_t *est,
-    VitalBandResult_t result,
-    uint16_t window_samples,
-    float bpm_min,
-    float bpm_max,
-    float bpm_step,
-    uint8_t diff_order)
-{
-    float mean;
-    float noise_sum = 0.0f;
-    float target_power;
-    float bpm;
-    uint16_t noise_count = 0U;
-    float exclusion_half_width = 0.5f * bpm_step + 0.001f;
+static void Vital_InsertCandidate(VitalSearchResult_t *result, const VitalCandidate_t *candidate)
+{
+    uint8_t i;
+    uint8_t j;
 
-    mean = Vital_ComputeMean(est, result.axis, window_samples);
-    target_power = Vital_GoertzelPowerWithMean(est,
-                                               result.axis,
-                                               window_samples,
-                                               mean,
-                                               result.bpm / 60.0f,
-                                               diff_order);
+    if ((result == 0) || (candidate == 0)) return;
 
-    for (bpm = bpm_min; bpm <= bpm_max + 0.01f; bpm += bpm_step)
+    /* Keep frequency-distinct peaks. If a close peak already exists, retain the better one. */
+    for (i = 0U; i < result->count; i++)
     {
-        if (Vital_AbsF(bpm - result.bpm) > exclusion_half_width)
+        if (Vital_AbsF(result->top[i].bpm - candidate->bpm) < 2.0f)
         {
-            noise_sum += Vital_GoertzelPowerWithMean(est,
-                                                     result.axis,
-                                                     window_samples,
-                                                     mean,
-                                                     bpm / 60.0f,
-                                                     diff_order);
-            noise_count++;
+            if (candidate->score <= result->top[i].score) return;
+            for (j = i; (j + 1U) < result->count; j++) result->top[j] = result->top[j + 1U];
+            result->count--;
+            break;
         }
     }
 
-    if (noise_count > 0U)
-    {
-        result.noise_power = noise_sum / (float)noise_count;
-    }
-    else
-    {
-        result.noise_power = VITAL_EPS;
-    }
+    if (result->count < VITAL_INTERNAL_CANDS) result->count++;
 
-    if (result.noise_power < VITAL_EPS)
+    for (i = 0U; i < result->count; i++)
     {
-        result.noise_power = VITAL_EPS;
+        if (candidate->score > result->top[i].score)
+        {
+            for (j = (uint8_t)(result->count - 1U); j > i; j--) result->top[j] = result->top[j - 1U];
+            result->top[i] = *candidate;
+            return;
+        }
     }
 
-    result.freq_hz = result.bpm / 60.0f;
-    result.peak_power = target_power;
-    result.snr = target_power / result.noise_power;
-    result.sqi = Vital_SnrToSqi(result.snr);
-    result.valid = 1U;
-    return result;
+    if (result->count > 0U) result->top[result->count - 1U] = *candidate;
 }
 
-static uint8_t Vital_IsNearRrHarmonic(float bpm, float rr_bpm, uint8_t *out_k)
+static float Vital_NoiseExcludingPeak(const float *p, uint8_t bins, uint8_t index)
 {
-    uint8_t k;
+    uint8_t i;
+    uint8_t count = 0U;
+    float sum = 0.0f;
 
-    if (out_k != 0)
+    for (i = 0U; i < bins; i++)
     {
-        *out_k = 0U;
-    }
+        int16_t d = (int16_t)i - (int16_t)index;
+        if ((d >= -2) && (d <= 2)) continue;
+        sum += p[i];
+        count++;
+    }
+
+    if (count == 0U) return VITAL_EPS;
+    sum /= (float)count;
+    return (sum > VITAL_EPS) ? sum : VITAL_EPS;
+}
+
+static VitalSearchResult_t Vital_SearchBand(const VitalEstimator_t *est,
+                                            uint16_t window_samples,
+                                            float bpm_min,
+                                            float bpm_max,
+                                            float bpm_step,
+                                            uint8_t diff_order,
+                                            uint8_t is_hr,
+                                            float track_bpm,
+                                            float rr_bpm,
+                                            uint8_t rr_sqi,
+                                            VitalAxis_t rr_axis)
+{
+    VitalSearchResult_t result;
+    float power[3][VITAL_MAX_BINS];
+    float means[3];
+    uint8_t bins = 0U;
+    uint8_t a;
+    uint8_t i;
+    float bpm;
+
+    memset(&result, 0, sizeof(result));
+    result.raw_best.axis = VITAL_AXIS_NONE;
+    result.clean_best.axis = VITAL_AXIS_NONE;
 
-    if (rr_bpm < 1.0f)
+    for (bpm = bpm_min; bpm <= bpm_max + 0.01f; bpm += bpm_step)
     {
-        return 0U;
+        if (bins < VITAL_MAX_BINS) bins++;
     }
 
-    for (k = VITAL_HR_HARMONIC_K_MIN; k <= VITAL_HR_HARMONIC_K_MAX; k++)
+    means[0] = Vital_ComputeMean(est, VITAL_AXIS_X, window_samples);
+    means[1] = Vital_ComputeMean(est, VITAL_AXIS_Y, window_samples);
+    means[2] = Vital_ComputeMean(est, VITAL_AXIS_Z, window_samples);
+
+    for (a = 0U; a < 3U; a++)
     {
-        float harmonic_bpm = rr_bpm * (float)k;
-        if (Vital_AbsF(bpm - harmonic_bpm) <= VITAL_HR_HARMONIC_TOL_BPM)
+        VitalAxis_t axis = (VitalAxis_t)a;
+        for (i = 0U; i < bins; i++)
+        {
+            float this_bpm = bpm_min + (float)i * bpm_step;
+            power[a][i] = Vital_GoertzelPowerWithMean(est,
+                                                      axis,
+                                                      window_samples,
+                                                      means[a],
+                                                      this_bpm / 60.0f,
+                                                      diff_order);
+        }
+    }
+
+    for (i = 0U; i < bins; i++)
+    {
+        float snr_axis[3];
+        float best_snr;
+        float second_snr;
+        uint8_t best_a = 0U;
+        float p;
+        float left;
+        float right;
+        float prominence;
+        float support;
+        float continuity;
+        float harmonic_quality = 1.0f;
+        float snr_quality;
+        float prom_quality;
+        float score;
+        float quality;
+        float transition_quality;
+        uint8_t near_harm = 0U;
+        uint8_t harmonic_k = 0U;
+        VitalCandidate_t cand;
+        uint8_t is_local_peak = 1U;
+
+        for (a = 0U; a < 3U; a++)
+        {
+            float noise = Vital_NoiseExcludingPeak(power[a], bins, i);
+            snr_axis[a] = power[a][i] / noise;
+        }
+
+        best_snr = snr_axis[0];
+        if (snr_axis[1] > best_snr) { best_snr = snr_axis[1]; best_a = 1U; }
+        if (snr_axis[2] > best_snr) { best_snr = snr_axis[2]; best_a = 2U; }
+
+        second_snr = 0.0f;
+        for (a = 0U; a < 3U; a++)
+        {
+            if ((a != best_a) && (snr_axis[a] > second_snr)) second_snr = snr_axis[a];
+        }
+
+        p = power[best_a][i];
+        left = (i > 0U) ? power[best_a][i - 1U] : p;
+        right = ((i + 1U) < bins) ? power[best_a][i + 1U] : p;
+        prominence = p / (0.5f * (left + right) + VITAL_EPS);
+        support = Vital_ClampF(second_snr / (best_snr + VITAL_EPS), 0.0f, 1.0f);
+
+        /* Local maxima only, while retaining edges for raw diagnostics. */
+        if ((i > 0U) && (p < power[best_a][i - 1U])) is_local_peak = 0U;
+        if (((i + 1U) < bins) && (p < power[best_a][i + 1U])) is_local_peak = 0U;
+
+        bpm = bpm_min + (float)i * bpm_step;
+        continuity = (is_hr != 0U) ? Vital_HrContinuityQuality(bpm, track_bpm)
+                                    : Vital_RrContinuityQuality(bpm, track_bpm);
+
+        if (is_hr != 0U)
+        {
+            harmonic_quality = Vital_HrHarmonicQuality(bpm,
+                                                       (VitalAxis_t)best_a,
+                                                       rr_bpm,
+                                                       rr_sqi,
+                                                       rr_axis,
+                                                       &near_harm,
+                                                       &harmonic_k);
+        }
+        else
         {
-            if (out_k != 0)
+            /* Soft fundamental-vs-2x ambiguity. Do not hard halve/double. */
+            float half_bpm = 0.5f * bpm;
+            if ((bpm >= 20.0f) && (half_bpm >= bpm_min))
             {
-                *out_k = k;
+                int16_t half_i = (int16_t)((half_bpm - bpm_min) / bpm_step + 0.5f);
+                if ((half_i >= 0) && (half_i < bins))
+                {
+                    float ratio = power[best_a][(uint8_t)half_i] / (p + VITAL_EPS);
+                    if (ratio > 0.65f) harmonic_quality = 0.55f;
+                    else if (ratio > 0.35f) harmonic_quality = 0.75f;
+                }
             }
-            return 1U;
         }
-    }
-
-    return 0U;
-}
 
-static void Vital_InsertHrCandidate(VitalHrSearchResult_t *result,
-                                     const VitalBandResult_t *cand,
-                                     uint8_t near_harm,
-                                     uint8_t harmonic_k,
-                                     float score)
-{
-    uint8_t i;
-    VitalHrCandidate_t item;
+        snr_quality = Vital_SnrQuality(best_snr, is_hr);
+        prom_quality = Vital_ProminenceQuality(prominence);
 
-    if ((result == 0) || (cand == 0))
-    {
-        return;
-    }
-
-    item.cand = *cand;
-    item.near_harmonic = near_harm;
-    item.harmonic_k = harmonic_k;
-    item.score = score;
-
-    for (i = 0U; i < VITAL_HR_CANDIDATE_COUNT; i++)
-    {
-        if ((result->top[i].cand.axis == VITAL_AXIS_NONE) || (score > result->top[i].score))
+        if (is_hr != 0U)
         {
-            uint8_t j;
-            for (j = (VITAL_HR_CANDIDATE_COUNT - 1U); j > i; j--)
-            {
-                result->top[j] = result->top[j - 1U];
-            }
-            result->top[i] = item;
-            break;
+            quality = 0.34f * snr_quality +
+                      0.20f * prom_quality +
+                      0.08f * support +
+                      0.25f * continuity +
+                      0.13f * harmonic_quality;
         }
-    }
-}
-
-static float Vital_HrContinuityScore(float bpm, float last_tracked_bpm)
-{
-    float delta;
+        else
+        {
+            quality = 0.36f * snr_quality +
+                      0.24f * prom_quality +
+                      0.08f * support +
+                      0.24f * continuity +
+                      0.08f * harmonic_quality;
+        }
+
+        /* Transition evidence deliberately excludes temporal continuity.
+         * A genuinely changing vital rate must be allowed to move away from the
+         * old track when the frequency itself is spectrally credible. */
+        if (is_hr != 0U)
+        {
+            transition_quality = 0.44f * snr_quality +
+                                 0.24f * prom_quality +
+                                 0.10f * support +
+                                 0.22f * harmonic_quality;
+        }
+        else
+        {
+            transition_quality = 0.48f * snr_quality +
+                                 0.27f * prom_quality +
+                                 0.10f * support +
+                                 0.15f * harmonic_quality;
+        }
+
+        /* Search edges are more vulnerable to leakage. */
+        if ((bpm <= bpm_min + 1.0f) || (bpm >= bpm_max - 1.0f))
+        {
+            quality *= 0.82f;
+            transition_quality *= 0.82f;
+        }
+
+        score = quality;
+        memset(&cand, 0, sizeof(cand));
+        cand.bpm = bpm;
+        cand.freq_hz = bpm / 60.0f;
+        cand.snr = best_snr;
+        cand.peak_power = p;
+        cand.noise_power = p / (best_snr + VITAL_EPS);
+        cand.prominence = prominence;
+        cand.axis_support = support;
+        cand.continuity_quality = continuity;
+        cand.harmonic_quality = harmonic_quality;
+        cand.transition_quality = transition_quality;
+        cand.score = score;
+        cand.sqi = Vital_QualityToSqi(quality);
+        cand.transition_sqi = Vital_QualityToSqi(transition_quality * VITAL_SWITCH_QUALITY_SCALE);
+        cand.axis = (VitalAxis_t)best_a;
+        cand.near_harmonic = near_harm;
+        cand.harmonic_k = harmonic_k;
+
+        if ((result.raw_best.axis == VITAL_AXIS_NONE) || (best_snr > result.raw_best.snr)) result.raw_best = cand;
+        if ((near_harm == 0U) && ((result.clean_best.axis == VITAL_AXIS_NONE) || (score > result.clean_best.score))) result.clean_best = cand;
 
-    if (last_tracked_bpm < 1.0f)
-    {
-        return 1.0f;
+        if (is_local_peak != 0U) Vital_InsertCandidate(&result, &cand);
     }
 
-    delta = Vital_AbsF(bpm - last_tracked_bpm);
-
-    if (delta <= VITAL_HR_TRACK_CONTINUITY_STRONG_BPM)
-    {
-        return 1.55f;
-    }
-    if (delta <= VITAL_HR_TRACK_CONTINUITY_WEAK_BPM)
+    /* A broad peak can miss strict local-max insertion at an edge; preserve best diagnostic candidate. */
+    if ((result.count == 0U) && (result.raw_best.axis != VITAL_AXIS_NONE))
     {
-        return 1.15f;
+        result.top[0] = result.raw_best;
+        result.count = 1U;
     }
 
-    return 0.58f;
+    return result;
 }
 
-static VitalHrSearchResult_t Vital_EstimateHrWithRrHarmonicGuard(const VitalEstimator_t *est,
-                                                                 float rr_bpm)
+static float Vital_RateLimitedUpdate(float track, float target, float alpha, float max_step)
 {
-    VitalHrSearchResult_t result;
-    VitalAxis_t axes[3] = { VITAL_AXIS_X, VITAL_AXIS_Y, VITAL_AXIS_Z };
-    uint8_t a;
-    uint8_t i;
+    float step = alpha * (target - track);
+    step = Vital_ClampF(step, -max_step, max_step);
+    return track + step;
+}
+
+static float Vital_UpdateTrack(float track,
+                               float candidate,
+                               uint8_t candidate_sqi,
+                               float switch_delta,
+                               uint8_t pending_required,
+                               float max_step,
+                               float *pending_bpm,
+                               uint8_t *pending_count,
+                               uint8_t *init_count,
+                               uint8_t is_rr)
+{
+    float delta;
+    float target;
+    float alpha;
 
-    memset(&result, 0, sizeof(result));
-    result.raw_best.axis = VITAL_AXIS_NONE;
-    result.clean_best.axis = VITAL_AXIS_NONE;
-    for (i = 0U; i < VITAL_HR_CANDIDATE_COUNT; i++)
-    {
-        result.top[i].cand.axis = VITAL_AXIS_NONE;
-    }
+    if ((pending_bpm == 0) || (pending_count == 0) || (init_count == 0)) return track;
 
-    for (a = 0U; a < 3U; a++)
+    if (track < 1.0f)
     {
-        VitalAxis_t axis = axes[a];
-        float mean = Vital_ComputeMean(est, axis, VITAL_HR_WINDOW_SAMPLES);
-        float total_power = 0.0f;
-        uint16_t bin_count = 0U;
-        float bpm;
-
-        for (bpm = VITAL_HR_MIN_BPM; bpm <= VITAL_HR_MAX_BPM + 0.01f; bpm += VITAL_HR_STEP_BPM)
+        if (candidate_sqi < 48U)
         {
-            float p = Vital_GoertzelPowerWithMean(est, axis, VITAL_HR_WINDOW_SAMPLES, mean, bpm / 60.0f, 1U);
-            total_power += p;
-            bin_count++;
+            *init_count = 0U;
+            return 0.0f;
         }
 
-        if (bin_count < 2U)
+        if ((*pending_bpm > 1.0f) && (Vital_AbsF(candidate - *pending_bpm) <= 3.0f))
         {
-            continue;
+            if (*init_count < 255U) (*init_count)++;
+            *pending_bpm = 0.5f * (*pending_bpm + candidate);
         }
-
-        for (bpm = VITAL_HR_MIN_BPM; bpm <= VITAL_HR_MAX_BPM + 0.01f; bpm += VITAL_HR_STEP_BPM)
+        else
         {
-            float p = Vital_GoertzelPowerWithMean(est, axis, VITAL_HR_WINDOW_SAMPLES, mean, bpm / 60.0f, 1U);
-            float noise = (total_power - p) / (float)(bin_count - 1U);
-            float snr;
-            float score;
-            uint8_t k = 0U;
-            uint8_t near_harm;
-            VitalBandResult_t cand;
-
-            if (noise < VITAL_EPS)
-            {
-                noise = VITAL_EPS;
-            }
-
-            snr = p / noise;
-            near_harm = Vital_IsNearRrHarmonic(bpm, rr_bpm, &k);
-
-            memset(&cand, 0, sizeof(cand));
-            cand.bpm = bpm;
-            cand.freq_hz = bpm / 60.0f;
-            cand.snr = snr;
-            cand.peak_power = p;
-            cand.noise_power = noise;
-            cand.axis = axis;
-            cand.sqi = Vital_SnrToSqi(snr);
-            cand.valid = 1U;
-
-            score = snr;
-
-            /* The HR search edges, especially 60 bpm in the human profile,
-             * often collect respiration leakage and RR harmonics. Keep the
-             * candidate visible for diagnostics, but make it hard to initialise
-             * or dominate the tracker from the boundary alone.
-             */
-            if ((bpm <= VITAL_HR_MIN_BPM + VITAL_HR_EDGE_GUARD_BPM) ||
-                (bpm >= VITAL_HR_MAX_BPM - VITAL_HR_EDGE_GUARD_BPM))
-            {
-                score *= VITAL_HR_EDGE_SCORE_PENALTY;
-            }
-
-            /* V4: do not kill RR harmonics immediately. For humans, true HR can
-             * sit close to 5x/6x RR. Reduce confidence but allow continuity to
-             * rescue it when it behaves like a stable HR track.
-             */
-            if (near_harm != 0U)
-            {
-                score *= 0.78f;
-            }
-
-            score *= Vital_HrContinuityScore(bpm, est->last_hr_tracked_bpm);
-
-            if ((result.raw_best.axis == VITAL_AXIS_NONE) || (snr > result.raw_best.snr))
-            {
-                result.raw_best = cand;
-                result.raw_near_harmonic = near_harm;
-                result.raw_harmonic_k = k;
-            }
-
-            if (near_harm == 0U)
-            {
-                if ((result.clean_best.axis == VITAL_AXIS_NONE) || (snr > result.clean_best.snr))
-                {
-                    result.clean_best = cand;
-                }
-            }
+            *pending_bpm = candidate;
+            *init_count = 1U;
+        }
 
-            Vital_InsertHrCandidate(&result, &cand, near_harm, k, score);
+        if (*init_count >= VITAL_TRACK_INIT_COUNT_REQUIRED)
+        {
+            track = *pending_bpm;
+            *pending_bpm = 0.0f;
+            *pending_count = 0U;
+            *init_count = 0U;
         }
+        return track;
     }
 
-    return result;
-}
+    if (candidate_sqi < 35U) return track;
 
-static VitalBandResult_t Vital_CorrectRrCandidate(const VitalEstimator_t *est,
-                                                  VitalBandResult_t rr,
-                                                  VitalOutput_t *out)
-{
-    float p_raw = rr.peak_power;
-    float p_alt = 0.0f;
-    float alt_bpm = 0.0f;
+    delta = Vital_AbsF(candidate - track);
+    target = candidate;
 
-    out->rr_raw_bpm = rr.bpm;
-    out->rr_alt_bpm = 0.0f;
-    out->rr_corrected = 0U;
+    if (delta > switch_delta)
+    {
+        if ((*pending_bpm > 1.0f) && (Vital_AbsF(candidate - *pending_bpm) <= 3.0f))
+        {
+            if (*pending_count < 255U) (*pending_count)++;
+            *pending_bpm = 0.65f * (*pending_bpm) + 0.35f * candidate;
+        }
+        else
+        {
+            *pending_bpm = candidate;
+            *pending_count = 1U;
+        }
 
-    if (p_raw < VITAL_EPS)
+        if ((*pending_count < pending_required) || (candidate_sqi < 62U))
+        {
+            target = track;
+        }
+        else
+        {
+            /* Once a distant candidate has been confirmed, keep following it
+             * at the configured rate limit. Do not require a fresh confirmation
+             * after every one-second tracker step. */
+            target = *pending_bpm;
+            *pending_count = pending_required;
+        }
+    }
+    else
     {
-        return rr;
+        *pending_count = 0U;
+        *pending_bpm = 0.0f;
     }
 
-    /* If the strongest peak is very slow, check whether it is actually a
-     * half-frequency breathing envelope and the real breath-by-breath peak is 2x.
-     */
-    if ((rr.bpm < VITAL_RR_LOW_BPM_FOR_DOUBLE_CHECK) &&
-        ((2.0f * rr.bpm) <= VITAL_RR_MAX_BPM))
-    {
-        alt_bpm = 2.0f * rr.bpm;
-        p_alt = Vital_GoertzelPower(est,
-                                    rr.axis,
-                                    VITAL_RR_WINDOW_SAMPLES,
-                                    alt_bpm / 60.0f,
-                                    0U);
-        out->rr_alt_bpm = alt_bpm;
-
-        if (p_alt >= (VITAL_RR_DOUBLE_POWER_RATIO_MIN * p_raw))
-        {
-            rr.bpm = alt_bpm;
-            out->rr_corrected = 1U; /* doubled */
-            return Vital_RecalculateBandMetricsAtBpm(est,
-                                                      rr,
-                                                      VITAL_RR_WINDOW_SAMPLES,
-                                                      VITAL_RR_MIN_BPM,
-                                                      VITAL_RR_MAX_BPM,
-                                                      VITAL_RR_STEP_BPM,
-                                                      0U);
-        }
-    }
-
-    /* If the strongest peak is high, check whether it is a 2x breathing harmonic. */
-    if ((rr.bpm > VITAL_RR_HIGH_BPM_FOR_HALF_CHECK) &&
-        ((0.5f * rr.bpm) >= VITAL_RR_MIN_BPM))
-    {
-        alt_bpm = 0.5f * rr.bpm;
-        p_alt = Vital_GoertzelPower(est,
-                                    rr.axis,
-                                    VITAL_RR_WINDOW_SAMPLES,
-                                    alt_bpm / 60.0f,
-                                    0U);
-        out->rr_alt_bpm = alt_bpm;
-
-        if (p_alt >= (VITAL_RR_HALF_POWER_RATIO_MIN * p_raw))
-        {
-            rr.bpm = alt_bpm;
-            out->rr_corrected = 2U; /* halved */
-            return Vital_RecalculateBandMetricsAtBpm(est,
-                                                      rr,
-                                                      VITAL_RR_WINDOW_SAMPLES,
-                                                      VITAL_RR_MIN_BPM,
-                                                      VITAL_RR_MAX_BPM,
-                                                      VITAL_RR_STEP_BPM,
-                                                      0U);
-        }
-    }
-
-    /* If history says RR was stable, prefer the previous RR when the new peak is
-     * a jump and previous-frequency power is still reasonably strong.
-     */
-    if (est->last_rr_bpm > 1.0f)
-    {
-        float delta = Vital_AbsF(rr.bpm - est->last_rr_bpm);
-        if (delta > VITAL_RR_STABLE_BPM_TOL)
-        {
-            float p_prev = Vital_GoertzelPower(est,
-                                               rr.axis,
-                                               VITAL_RR_WINDOW_SAMPLES,
-                                               est->last_rr_bpm / 60.0f,
-                                               0U);
-            if (p_prev >= (VITAL_RR_HISTORY_KEEP_POWER_RATIO_MIN * p_raw))
-            {
-                rr.bpm = est->last_rr_bpm;
-                out->rr_alt_bpm = est->last_rr_bpm;
-                out->rr_corrected = 3U; /* history locked */
-                return Vital_RecalculateBandMetricsAtBpm(est,
-                                                          rr,
-                                                          VITAL_RR_WINDOW_SAMPLES,
-                                                          VITAL_RR_MIN_BPM,
-                                                          VITAL_RR_MAX_BPM,
-                                                          VITAL_RR_STEP_BPM,
-                                                          0U);
-            }
-        }
+    delta = Vital_AbsF(target - track);
+    if (is_rr != 0U)
+    {
+        if (delta <= 2.0f) alpha = 0.35f;
+        else if (delta <= switch_delta) alpha = 0.20f;
+        else alpha = 0.30f;
+    }
+    else
+    {
+        if (delta <= 2.0f) alpha = 0.55f;
+        else if (delta <= switch_delta) alpha = 0.32f;
+        else alpha = 0.30f;
     }
 
-    return rr;
+    return Vital_RateLimitedUpdate(track, target, alpha, max_step);
 }
 
 void VitalEstimator_Init(VitalEstimator_t *est)
 {
-    if (est == 0)
-    {
-        return;
-    }
-
+    if (est == 0) return;
     memset(est, 0, sizeof(*est));
+    est->motion_quality = 1.0f;
+    est->motion_class = VITAL_MOTION_CLEAN;
     est->last_output.rr_axis = VITAL_AXIS_NONE;
     est->last_output.hr_axis = VITAL_AXIS_NONE;
     est->last_output.hr_status = VITAL_HR_STATUS_NONE;
@@ -649,27 +617,15 @@
 
 void VitalEstimator_Reset(VitalEstimator_t *est)
 {
-    if (est == 0)
-    {
-        return;
-    }
-
     VitalEstimator_Init(est);
 }
 
 void VitalEstimator_PushSample(VitalEstimator_t *est, const ImuSample_t *sample)
 {
-    float dx;
-    float dy;
-    float dz;
-    int16_t qx;
-    int16_t qy;
-    int16_t qz;
+    float dx, dy, dz;
+    int16_t qx, qy, qz;
 
-    if ((est == 0) || (sample == 0))
-    {
-        return;
-    }
+    if ((est == 0) || (sample == 0)) return;
 
     if (est->gravity_initialised == 0U)
     {
@@ -692,10 +648,7 @@
     est->aa_z += VITAL_AA_ALPHA * (dz - est->aa_z);
 
     est->decim_count++;
-    if (est->decim_count < VITAL_DECIMATION_FACTOR)
-    {
-        return;
-    }
+    if (est->decim_count < VITAL_DECIMATION_FACTOR) return;
     est->decim_count = 0U;
 
     qx = Vital_ClampI16FromFloat(est->aa_x * VITAL_Q_SCALE);
@@ -705,33 +658,75 @@
     est->x[est->write_index] = qx;
     est->y[est->write_index] = qy;
     est->z[est->write_index] = qz;
-
     est->write_index++;
-    if (est->write_index >= VITAL_WINDOW_SAMPLES)
+    if (est->write_index >= VITAL_WINDOW_SAMPLES) est->write_index = 0U;
+    if (est->count < VITAL_WINDOW_SAMPLES) est->count++;
+}
+
+void VitalEstimator_SetMotionContext(VitalEstimator_t *est, const RestDetectorOutput_t *rest_out)
+{
+    const RestShortFeatures_t *f;
+    float quality = 1.0f;
+    VitalMotionClass_t motion_class = VITAL_MOTION_CLEAN;
+
+    if ((est == 0) || (rest_out == 0)) return;
+    f = &rest_out->features_2s;
+
+    if (rest_out->strong_motion_detected)
+    {
+        quality = 0.0f;
+        motion_class = VITAL_MOTION_STRONG;
+        est->motion_hold_seconds = VITAL_MOTION_RECOVERY_HOLD_SEC;
+    }
+    else if ((f->acc_mag_range >= 0.25f) ||
+             (f->acc_jerk_p95 >= 3.5f) ||
+             (f->gyr_mag_p95 >= 35.0f))
+    {
+        quality = 0.18f;
+        motion_class = VITAL_MOTION_MODERATE;
+        est->motion_hold_seconds = VITAL_MOTION_RECOVERY_HOLD_SEC;
+    }
+    else if ((rest_out->local_label == REST_LOCAL_DISTURBANCE) ||
+             (f->acc_mag_range >= 0.05f) ||
+             (f->acc_jerk_p95 >= 1.5f) ||
+             (f->gyr_mag_p95 >= 8.0f))
+    {
+        quality = 0.48f;
+        motion_class = VITAL_MOTION_MILD;
+        if (est->motion_hold_seconds < 2U) est->motion_hold_seconds = 2U;
+    }
+    else if ((f->gyr_mag_p95 >= 3.0f) || (f->gyr_mag_rms >= 1.8f))
     {
-        est->write_index = 0U;
+        /* Sustained low-amplitude sway: remain measurable with a small SQI cost. */
+        quality = 0.90f;
+        motion_class = VITAL_MOTION_MILD;
     }
 
-    if (est->count < VITAL_WINDOW_SAMPLES)
+    if ((motion_class < VITAL_MOTION_MODERATE) && (est->motion_hold_seconds > 0U))
     {
-        est->count++;
+        const uint8_t remaining = est->motion_hold_seconds;
+        const float recovery_cap =
+            (remaining > (VITAL_MOTION_RECOVERY_HOLD_SEC / 2U)) ? 0.38f : 0.58f;
+        est->motion_hold_seconds--;
+        if (quality > recovery_cap) quality = recovery_cap;
+        motion_class = VITAL_MOTION_MILD;
     }
+
+    est->motion_quality = quality;
+    est->motion_class = motion_class;
 }
 
 bool VitalEstimator_Estimate(VitalEstimator_t *est, VitalOutput_t *out)
 {
-    VitalBandResult_t rr;
-    VitalHrSearchResult_t hr_search;
-    VitalHrCandidate_t hr_selected;
-    uint8_t hr_stable_now = 0U;
-    uint8_t rr_window_ready;
-    uint8_t tracker_init_allowed = 0U;
+    VitalSearchResult_t rr_search;
+    VitalSearchResult_t hr_search;
+    VitalCandidate_t rr_cand;
+    VitalCandidate_t hr_cand;
     uint8_t i;
+    float rr_quality;
+    float hr_quality;
 
-    if ((est == 0) || (out == 0))
-    {
-        return false;
-    }
+    if ((est == 0) || (out == 0)) return false;
 
     memset(out, 0, sizeof(*out));
     out->rr_axis = VITAL_AXIS_NONE;
@@ -739,173 +734,148 @@
     out->hr_status = VITAL_HR_STATUS_NONE;
     out->stored_samples = est->count;
     out->window_fill_percent = (uint8_t)(((uint32_t)est->count * 100U) / VITAL_WINDOW_SAMPLES);
-    rr_window_ready = (est->count >= VITAL_RR_WINDOW_SAMPLES) ? 1U : 0U;
+    out->motion_quality = est->motion_quality;
+    out->motion_class = est->motion_class;
 
-    if (est->count < VITAL_HR_WINDOW_SAMPLES)
+    if (est->count < VITAL_WINDOW_SAMPLES)
     {
         est->last_output = *out;
         return false;
     }
 
-    if (est->count >= VITAL_RR_WINDOW_SAMPLES)
-    {
-        rr = Vital_EstimateBandAllAxes(est,
-                                       VITAL_RR_WINDOW_SAMPLES,
-                                       VITAL_RR_MIN_BPM,
-                                       VITAL_RR_MAX_BPM,
-                                       VITAL_RR_STEP_BPM,
-                                       0U);
-
-        rr = Vital_CorrectRrCandidate(est, rr, out);
-
-        out->rr_bpm = rr.bpm;
-        out->rr_peak_freq_hz = rr.freq_hz;
-        out->rr_snr = rr.snr;
-        out->rr_sqi = rr.sqi;
-        out->rr_axis = rr.axis;
-        out->rr_energy_low = (rr.peak_power < VITAL_RR_PEAK_POWER_MIN) ? 1U : 0U;
-        out->rr_valid = ((rr.snr >= VITAL_RR_SNR_MIN) && (out->rr_energy_low == 0U)) ? 1U : 0U;
-
-        if (out->rr_valid != 0U)
-        {
-            est->last_rr_bpm = out->rr_bpm;
-        }
-    }
-
-    hr_search = Vital_EstimateHrWithRrHarmonicGuard(est, (out->rr_valid != 0U) ? out->rr_bpm : 0.0f);
-
+    rr_search = Vital_SearchBand(est,
+                                 VITAL_RR_WINDOW_SAMPLES,
+                                 VITAL_RR_MIN_BPM,
+                                 VITAL_RR_MAX_BPM,
+                                 VITAL_RR_STEP_BPM,
+                                 0U,
+                                 0U,
+                                 est->rr_track_bpm,
+                                 0.0f,
+                                 0U,
+                                 VITAL_AXIS_NONE);
+
+    rr_cand = rr_search.top[0];
+    rr_quality = ((float)rr_cand.sqi / 100.0f) * est->motion_quality;
+    rr_cand.sqi = Vital_QualityToSqi(rr_quality);
+
+    {
+        const uint8_t rr_switch_sqi =
+            Vital_QualityToSqi(((float)rr_cand.transition_sqi / 100.0f) * est->motion_quality);
+        est->rr_track_bpm = Vital_UpdateTrack(est->rr_track_bpm,
+                                          rr_cand.bpm,
+                                          rr_switch_sqi,
+                                          VITAL_RR_SWITCH_DELTA_BPM,
+                                          VITAL_RR_PENDING_COUNT_REQUIRED,
+                                          VITAL_RR_MAX_STEP_BPM_PER_EST,
+                                          &est->rr_pending_bpm,
+                                          &est->rr_pending_count,
+                                          &est->rr_init_count,
+                                          1U);
+    }
+
+    /* SQI describes the final tracked output, not only the raw spectral peak.
+     * During a pending/slew transition, reduce confidence in proportion to the
+     * disagreement between the output and the current best candidate. */
+    if (est->rr_track_bpm > 0.0f)
+    {
+        float agreement = Vital_ClampF(1.0f - Vital_AbsF(est->rr_track_bpm - rr_cand.bpm) / 8.0f, 0.25f, 1.0f);
+        rr_cand.sqi = Vital_QualityToSqi(((float)rr_cand.sqi / 100.0f) * agreement);
+    }
+
+    out->rr_candidate_bpm = rr_cand.bpm;
+    out->rr_raw_bpm = rr_search.raw_best.bpm;
+    out->rr_alt_bpm = (rr_search.count > 1U) ? rr_search.top[1].bpm : 0.0f;
+    out->rr_corrected = 0U;
+    out->rr_energy_low = (rr_cand.peak_power < VITAL_RR_PEAK_POWER_MIN) ? 1U : 0U;
+    out->rr_bpm = est->rr_track_bpm;
+    out->rr_peak_freq_hz = (est->rr_track_bpm > 0.0f) ? est->rr_track_bpm / 60.0f : 0.0f;
+    out->rr_snr = rr_cand.snr;
+    out->rr_sqi = rr_cand.sqi;
+    out->rr_axis = rr_cand.axis;
+    out->rr_prominence = rr_cand.prominence;
+    out->rr_axis_support = rr_cand.axis_support;
+    out->rr_continuity_quality = rr_cand.continuity_quality;
+    out->rr_harmonic_quality = rr_cand.harmonic_quality;
+    out->rr_valid = ((out->rr_bpm > 0.0f) &&
+                     (out->rr_sqi >= VITAL_RR_VALID_SQI_MIN) &&
+                     (out->rr_energy_low == 0U)) ? 1U : 0U;
+
+    hr_search = Vital_SearchBand(est,
+                                 VITAL_HR_WINDOW_SAMPLES,
+                                 VITAL_HR_MIN_BPM,
+                                 VITAL_HR_MAX_BPM,
+                                 VITAL_HR_STEP_BPM,
+                                 1U,
+                                 1U,
+                                 est->hr_track_bpm,
+                                 (out->rr_bpm > 0.0f) ? out->rr_bpm : rr_cand.bpm,
+                                 out->rr_sqi,
+                                 out->rr_axis);
+
+    hr_cand = hr_search.top[0];
+    hr_quality = ((float)hr_cand.sqi / 100.0f) * est->motion_quality;
+    hr_cand.sqi = Vital_QualityToSqi(hr_quality);
+
+    est->hr_track_bpm = Vital_UpdateTrack(est->hr_track_bpm,
+                                          hr_cand.bpm,
+                                          hr_cand.sqi,
+                                          VITAL_HR_SWITCH_DELTA_BPM,
+                                          VITAL_HR_PENDING_COUNT_REQUIRED,
+                                          VITAL_HR_MAX_STEP_BPM_PER_EST,
+                                          &est->hr_pending_bpm,
+                                          &est->hr_pending_count,
+                                          &est->hr_init_count,
+                                          0U);
+
+    if (est->hr_track_bpm > 0.0f)
+    {
+        float agreement = Vital_ClampF(1.0f - Vital_AbsF(est->hr_track_bpm - hr_cand.bpm) / 18.0f, 0.20f, 1.0f);
+        hr_cand.sqi = Vital_QualityToSqi(((float)hr_cand.sqi / 100.0f) * agreement);
+    }
+
+    out->hr_candidate_bpm = hr_cand.bpm;
+    out->hr_bpm = est->hr_track_bpm;
+    out->hr_tracked_bpm = est->hr_track_bpm;
+    out->hr_peak_freq_hz = (est->hr_track_bpm > 0.0f) ? est->hr_track_bpm / 60.0f : 0.0f;
+    out->hr_snr = hr_cand.snr;
+    out->hr_sqi = hr_cand.sqi;
+    out->hr_axis = hr_cand.axis;
     out->hr_raw_bpm = hr_search.raw_best.bpm;
     out->hr_raw_snr = hr_search.raw_best.snr;
     out->hr_clean_bpm = hr_search.clean_best.bpm;
     out->hr_clean_snr = hr_search.clean_best.snr;
-    out->hr_near_harmonic = hr_search.raw_near_harmonic;
-    out->hr_harmonic_k = hr_search.raw_harmonic_k;
+    out->hr_near_harmonic = hr_cand.near_harmonic;
+    out->hr_harmonic_k = hr_cand.harmonic_k;
+    out->hr_prominence = hr_cand.prominence;
+    out->hr_axis_support = hr_cand.axis_support;
+    out->hr_continuity_quality = hr_cand.continuity_quality;
+    out->hr_harmonic_quality = hr_cand.harmonic_quality;
 
     for (i = 0U; i < VITAL_HR_CANDIDATE_COUNT; i++)
     {
-        out->hr_cand_bpm[i] = hr_search.top[i].cand.bpm;
-        out->hr_cand_snr[i] = hr_search.top[i].cand.snr;
-        out->hr_cand_harm_k[i] = hr_search.top[i].harmonic_k;
-    }
-
-    hr_selected = hr_search.top[0];
-    if (hr_selected.cand.axis == VITAL_AXIS_NONE)
-    {
-        est->last_output = *out;
-        return true;
-    }
-
-    out->hr_bpm = hr_selected.cand.bpm;
-    out->hr_peak_freq_hz = hr_selected.cand.freq_hz;
-    out->hr_snr = hr_selected.cand.snr;
-    out->hr_sqi = Vital_SnrToSqi(hr_selected.cand.snr);
-    out->hr_axis = hr_selected.cand.axis;
-    out->hr_near_harmonic = hr_selected.near_harmonic;
-    out->hr_harmonic_k = hr_selected.harmonic_k;
-
-    if (est->last_hr_candidate_bpm > 1.0f)
-    {
-        if (Vital_AbsF(hr_selected.cand.bpm - est->last_hr_candidate_bpm) <= VITAL_HR_STABLE_BPM_TOL)
-        {
-            if (est->hr_stable_count < 255U)
-            {
-                est->hr_stable_count++;
-            }
-        }
-        else
-        {
-            est->hr_stable_count = 0U;
-        }
-    }
-
-    est->last_hr_candidate_bpm = hr_selected.cand.bpm;
-    hr_stable_now = (est->hr_stable_count >= VITAL_HR_STABLE_COUNT_REQUIRED) ? 1U : 0U;
-
-    if (est->last_hr_tracked_bpm < 1.0f)
-    {
-        tracker_init_allowed =
-            ((rr_window_ready != 0U) &&
-             (hr_selected.cand.snr >= VITAL_HR_TRACK_INIT_SNR_MIN) &&
-#if VITAL_HR_TRACK_INIT_REQUIRE_STABLE
-             (hr_stable_now != 0U) &&
-#endif
-             /* When RR is valid, do not initialise the persistent track from
-              * a candidate already identified as an RR harmonic. */
-             ((out->rr_valid == 0U) || (hr_selected.near_harmonic == 0U))) ? 1U : 0U;
-
-        if (tracker_init_allowed != 0U)
+        if (i < hr_search.count)
         {
-            est->last_hr_tracked_bpm = hr_selected.cand.bpm;
+            out->hr_cand_bpm[i] = hr_search.top[i].bpm;
+            out->hr_cand_snr[i] = hr_search.top[i].snr;
+            out->hr_cand_harm_k[i] = hr_search.top[i].harmonic_k;
         }
     }
-    else if ((hr_selected.cand.snr >= VITAL_HR_SNR_LOW_MIN) &&
-             ((hr_selected.near_harmonic == 0U) ||
-              (Vital_AbsF(hr_selected.cand.bpm - est->last_hr_tracked_bpm) <=
-               VITAL_HR_TRACK_CONTINUITY_WEAK_BPM)) &&
-             ((hr_stable_now != 0U) || (Vital_AbsF(hr_selected.cand.bpm - est->last_hr_tracked_bpm) <= VITAL_HR_TRACK_CONTINUITY_WEAK_BPM)))
-    {
-        est->last_hr_tracked_bpm = (1.0f - VITAL_HR_TRACK_ALPHA) * est->last_hr_tracked_bpm +
-                                   VITAL_HR_TRACK_ALPHA * hr_selected.cand.bpm;
-    }
-
-    out->hr_tracked_bpm = est->last_hr_tracked_bpm;
 
-    /* The 30-40 s interval is intentionally candidate-only. RR has not yet
-     * had its full window, so respiration-harmonic rejection is unavailable.
-     * Report a strong stable candidate as VALID_LOW, but never as final VALID.
-     */
-    if (rr_window_ready == 0U)
-    {
-        out->hr_valid = 0U;
-        if ((hr_selected.cand.snr >= VITAL_HR_SNR_LOW_MIN) &&
-            (hr_stable_now != 0U))
-        {
-            out->hr_status = VITAL_HR_STATUS_VALID_LOW;
-        }
-        else
-        {
-            out->hr_status = VITAL_HR_STATUS_NONE;
-        }
-
-        est->last_output = *out;
-        return true;
-    }
-
-    if ((hr_selected.cand.snr >= VITAL_HR_SNR_MIN) &&
-        (hr_stable_now != 0U) &&
-        (hr_selected.near_harmonic == 0U))
+    if ((out->hr_bpm > 0.0f) && (out->hr_sqi >= VITAL_HR_VALID_SQI_MIN))
     {
         out->hr_valid = 1U;
         out->hr_status = VITAL_HR_STATUS_VALID;
     }
-    else if ((hr_selected.cand.snr >= VITAL_HR_SNR_MIN) &&
-             (hr_stable_now != 0U) &&
-             (hr_selected.near_harmonic != 0U))
-    {
-        /* Stable but near RR harmonic: plausible HR under normal breathing, but
-         * mark as lower confidence rather than hard CONTAM.
-         */
-        out->hr_valid = 0U;
-        out->hr_status = VITAL_HR_STATUS_VALID_LOW;
-    }
-    else if ((hr_selected.cand.snr >= VITAL_HR_SNR_LOW_MIN) &&
-             (Vital_AbsF(hr_selected.cand.bpm - est->last_hr_tracked_bpm) <= VITAL_HR_TRACK_CONTINUITY_WEAK_BPM))
+    else if ((out->hr_bpm > 0.0f) && (out->hr_sqi >= VITAL_HR_VALID_LOW_SQI_MIN))
     {
         out->hr_valid = 0U;
         out->hr_status = VITAL_HR_STATUS_VALID_LOW;
     }
-    else if ((hr_search.raw_near_harmonic != 0U) &&
-             (hr_search.raw_best.snr >= VITAL_HR_CONTAM_IF_RAW_SNR_MIN))
+    else if ((est->motion_class >= VITAL_MOTION_MODERATE) && (out->hr_bpm > 0.0f))
     {
         out->hr_valid = 0U;
         out->hr_status = VITAL_HR_STATUS_CONTAM;
-        out->hr_bpm = hr_search.raw_best.bpm;
-        out->hr_peak_freq_hz = hr_search.raw_best.freq_hz;
-        out->hr_snr = hr_search.raw_best.snr;
-        out->hr_sqi = Vital_SnrToSqi(hr_search.raw_best.snr);
-        out->hr_axis = hr_search.raw_best.axis;
-        out->hr_near_harmonic = 1U;
-        out->hr_harmonic_k = hr_search.raw_harmonic_k;
     }
     else
     {
@@ -938,3 +908,15 @@
         default: return "----";
     }
 }
+
+const char *VitalEstimator_MotionClassToString(VitalMotionClass_t motion_class)
+{
+    switch (motion_class)
+    {
+        case VITAL_MOTION_CLEAN: return "CLEAN";
+        case VITAL_MOTION_MILD: return "MILD";
+        case VITAL_MOTION_MODERATE: return "MODERATE";
+        case VITAL_MOTION_STRONG: return "STRONG";
+        default: return "UNKNOWN";
+    }
+}
diff -ruN '--exclude=build' '--exclude=*.exe' '--exclude=*.csv' '--exclude=*.png' /mnt/data/pet_algo_work/baseline/Pet_Collar_PC_Baseline/algorithm/vital_estimator.h /mnt/data/Pet_Collar_PC_Improved_V14/algorithm/vital_estimator.h
--- /mnt/data/pet_algo_work/baseline/Pet_Collar_PC_Baseline/algorithm/vital_estimator.h	2026-07-13 17:15:23.000000000 +0000
+++ /mnt/data/Pet_Collar_PC_Improved_V14/algorithm/vital_estimator.h	2026-07-16 01:05:06.690335206 +0000
@@ -6,31 +6,30 @@
 #include "rest_detector.h"
 
 /* ============================================================
- * Vital estimator configuration - V4 human test
+ * Vital estimator configuration - V14 cross-dataset candidate
  * ============================================================
- * Input IMU sample rate is expected to be 100 Hz.
- * Internally, dynamic acceleration is decimated to 26 Hz and
- * stored as int16_t to keep RAM usage low.
- *
- * V4 changes compared with V3:
- *   - HR keeps top-3 candidates instead of only strongest peak.
- *   - HR candidate selection uses continuity tracking.
- *   - HR can be VALID, VALID_LOW, CONTAM, or NONE.
- *   - RR has low-energy guard to avoid forcing RR during breath hold.
+ * - 100 Hz input, decimated by 4 to 25 Hz.
+ * - 18 s HR window and 10 s RR window to reduce latency.
+ * - One estimate per second.
+ * - Candidate quality combines SNR, local peak prominence,
+ *   multi-axis support, temporal continuity, harmonic ambiguity,
+ *   and motion context.
+ * - Harmonics affect SQI/score; they do not directly decide valid.
+ * - Final HR/RR are tracked and rate-limited outputs.
+ * - RR transition evidence excludes continuity so true changes can leave the old track.
+ * - RR local updates are deliberately smoother than confirmed distant transitions.
  */
 
 #define VITAL_INPUT_FS_HZ                100U
 #define VITAL_DECIMATION_FACTOR          4U
 #define VITAL_FS_HZ                      (VITAL_INPUT_FS_HZ / VITAL_DECIMATION_FACTOR)
 
-#define VITAL_WINDOW_SEC                 40U
-#define VITAL_WINDOW_SAMPLES             (VITAL_FS_HZ * VITAL_WINDOW_SEC)       /* 1000 */
-
-#define VITAL_HR_WINDOW_SEC              30U
-#define VITAL_HR_WINDOW_SAMPLES          (VITAL_FS_HZ * VITAL_HR_WINDOW_SEC)    /* 750 */
-
-#define VITAL_RR_WINDOW_SEC              40U
-#define VITAL_RR_WINDOW_SAMPLES          (VITAL_FS_HZ * VITAL_RR_WINDOW_SEC)    /* 1000 */
+#define VITAL_HR_WINDOW_SEC              18U
+#define VITAL_RR_WINDOW_SEC              10U
+#define VITAL_WINDOW_SEC                 18U
+#define VITAL_WINDOW_SAMPLES             (VITAL_FS_HZ * VITAL_WINDOW_SEC)
+#define VITAL_HR_WINDOW_SAMPLES          (VITAL_FS_HZ * VITAL_HR_WINDOW_SEC)
+#define VITAL_RR_WINDOW_SAMPLES          (VITAL_FS_HZ * VITAL_RR_WINDOW_SEC)
 
 #define VITAL_PROFILE_HUMAN_TEST         1U
 
@@ -48,52 +47,26 @@
 
 #define VITAL_RR_STEP_BPM                1.0f
 #define VITAL_HR_STEP_BPM                1.0f
-
 #define VITAL_Q_SCALE                    131072.0f
+#define VITAL_HR_CANDIDATE_COUNT         3U
+#define VITAL_ESTIMATE_PERIOD_SEC        1U
 
-/* Signal-quality thresholds. */
-#define VITAL_RR_SNR_MIN                 4.2f
-#define VITAL_RR_PEAK_POWER_MIN          1.0e-10f
-
-#define VITAL_HR_SNR_MIN                 5.5f
-#define VITAL_HR_SNR_LOW_MIN             4.2f
-#define VITAL_HR_STABLE_COUNT_REQUIRED   2U
-#define VITAL_HR_STABLE_BPM_TOL          12.0f
-#define VITAL_RR_STABLE_BPM_TOL          4.0f
-
-#define VITAL_RR_LOW_BPM_FOR_DOUBLE_CHECK        12.0f
-#define VITAL_RR_HIGH_BPM_FOR_HALF_CHECK         24.0f
-#define VITAL_RR_DOUBLE_POWER_RATIO_MIN          0.30f
-#define VITAL_RR_HALF_POWER_RATIO_MIN            0.45f
-#define VITAL_RR_HISTORY_KEEP_POWER_RATIO_MIN    0.55f
-
-#define VITAL_HR_HARMONIC_K_MIN                  2U
-#define VITAL_HR_HARMONIC_K_MAX                  12U
-#define VITAL_HR_HARMONIC_TOL_BPM                3.0f
-#define VITAL_HR_CONTAM_IF_RAW_SNR_MIN           4.8f
-#define VITAL_HR_EDGE_GUARD_BPM                  2.0f
-#define VITAL_HR_EDGE_SCORE_PENALTY              0.18f
-
-#define VITAL_HR_TRACK_ALPHA                      0.25f
-#define VITAL_HR_TRACK_CONTINUITY_STRONG_BPM      8.0f
-#define VITAL_HR_TRACK_CONTINUITY_WEAK_BPM        18.0f
-
-/*
- * Conservative tracker initialisation:
- * - HR candidates may be reported once the 30 s HR window is ready;
- * - the persistent tracker is not initialised until the 40 s RR window exists;
- * - the candidate must already be stable and meet the normal HR SNR threshold.
- */
-#define VITAL_HR_TRACK_INIT_SNR_MIN                VITAL_HR_SNR_MIN
-#define VITAL_HR_TRACK_INIT_REQUIRE_STABLE         1U
-
-#define VITAL_HR_CANDIDATE_COUNT                  3U
-
-#define VITAL_ESTIMATE_PERIOD_SEC        2U
-
-/* ============================================================
- * Types
- * ============================================================ */
+/* Final output thresholds. */
+#define VITAL_RR_VALID_SQI_MIN            55U
+#define VITAL_HR_VALID_SQI_MIN            65U
+#define VITAL_HR_VALID_LOW_SQI_MIN        45U
+#define VITAL_RR_PEAK_POWER_MIN           1.0e-10f
+
+/* Tracker behaviour, expressed per one-second estimate. */
+#define VITAL_HR_MAX_STEP_BPM_PER_EST     2.00f
+#define VITAL_RR_MAX_STEP_BPM_PER_EST     2.00f
+#define VITAL_HR_SWITCH_DELTA_BPM         8.00f
+#define VITAL_RR_SWITCH_DELTA_BPM         4.0f
+#define VITAL_HR_PENDING_COUNT_REQUIRED   4U
+#define VITAL_RR_PENDING_COUNT_REQUIRED   2U
+#define VITAL_TRACK_INIT_COUNT_REQUIRED   2U
+#define VITAL_MOTION_RECOVERY_HOLD_SEC     10U
+#define VITAL_SWITCH_QUALITY_SCALE         1.00f
 
 typedef enum
 {
@@ -111,13 +84,21 @@
     VITAL_HR_STATUS_VALID_LOW = 3
 } VitalHrStatus_t;
 
+typedef enum
+{
+    VITAL_MOTION_CLEAN = 0,
+    VITAL_MOTION_MILD = 1,
+    VITAL_MOTION_MODERATE = 2,
+    VITAL_MOTION_STRONG = 3
+} VitalMotionClass_t;
+
 typedef struct
 {
     float rr_bpm;
     float hr_bpm;
 
     uint8_t rr_valid;
-    uint8_t hr_valid;      /* 1 only when HR_STATUS_VALID */
+    uint8_t hr_valid;
     VitalHrStatus_t hr_status;
 
     uint8_t rr_sqi;
@@ -136,7 +117,8 @@
     uint8_t window_fill_percent;
     uint16_t stored_samples;
 
-    uint8_t rr_corrected;          /* 0 none, 1 doubled, 2 halved, 3 history-locked */
+    /* Kept for CSV compatibility. Hard x2,/2/history correction is removed. */
+    uint8_t rr_corrected;
     uint8_t rr_energy_low;
     float rr_raw_bpm;
     float rr_alt_bpm;
@@ -151,6 +133,22 @@
     float hr_cand_bpm[VITAL_HR_CANDIDATE_COUNT];
     float hr_cand_snr[VITAL_HR_CANDIDATE_COUNT];
     uint8_t hr_cand_harm_k[VITAL_HR_CANDIDATE_COUNT];
+
+    /* V14 quality diagnostics and regression fields. */
+    float motion_quality;
+    VitalMotionClass_t motion_class;
+
+    float rr_candidate_bpm;
+    float rr_prominence;
+    float rr_axis_support;
+    float rr_continuity_quality;
+    float rr_harmonic_quality;
+
+    float hr_candidate_bpm;
+    float hr_prominence;
+    float hr_axis_support;
+    float hr_continuity_quality;
+    float hr_harmonic_quality;
 } VitalOutput_t;
 
 typedef struct
@@ -165,18 +163,25 @@
     float gravity_x;
     float gravity_y;
     float gravity_z;
-
     float aa_x;
     float aa_y;
     float aa_z;
-
     uint8_t decim_count;
     uint8_t gravity_initialised;
 
-    float last_rr_bpm;
-    float last_hr_candidate_bpm;
-    float last_hr_tracked_bpm;
-    uint8_t hr_stable_count;
+    float rr_track_bpm;
+    float rr_pending_bpm;
+    uint8_t rr_pending_count;
+    uint8_t rr_init_count;
+
+    float hr_track_bpm;
+    float hr_pending_bpm;
+    uint8_t hr_pending_count;
+    uint8_t hr_init_count;
+
+    float motion_quality;
+    VitalMotionClass_t motion_class;
+    uint8_t motion_hold_seconds;
 
     VitalOutput_t last_output;
 } VitalEstimator_t;
@@ -184,9 +189,11 @@
 void VitalEstimator_Init(VitalEstimator_t *est);
 void VitalEstimator_Reset(VitalEstimator_t *est);
 void VitalEstimator_PushSample(VitalEstimator_t *est, const ImuSample_t *sample);
+void VitalEstimator_SetMotionContext(VitalEstimator_t *est, const RestDetectorOutput_t *rest_out);
 bool VitalEstimator_Estimate(VitalEstimator_t *est, VitalOutput_t *out);
 
 const char *VitalEstimator_AxisToString(VitalAxis_t axis);
 const char *VitalEstimator_HrStatusToString(VitalHrStatus_t status);
+const char *VitalEstimator_MotionClassToString(VitalMotionClass_t motion_class);
 
 #endif /* VITAL_ESTIMATOR_H */
diff -ruN '--exclude=build' '--exclude=*.exe' '--exclude=*.csv' '--exclude=*.png' /mnt/data/pet_algo_work/baseline/Pet_Collar_PC_Baseline/src/main_pc.c /mnt/data/Pet_Collar_PC_Improved_V14/src/main_pc.c
--- /mnt/data/pet_algo_work/baseline/Pet_Collar_PC_Baseline/src/main_pc.c	2026-07-13 18:47:25.000000000 +0000
+++ /mnt/data/Pet_Collar_PC_Improved_V14/src/main_pc.c	2026-07-16 01:00:23.552749533 +0000
@@ -58,7 +58,10 @@
             "hr_raw_bpm,hr_raw_snr,hr_clean_bpm,hr_clean_snr,hr_near_harmonic,hr_harmonic_k,"
             "hr_cand1_bpm,hr_cand1_snr,hr_cand1_harm_k,"
             "hr_cand2_bpm,hr_cand2_snr,hr_cand2_harm_k,"
-            "hr_cand3_bpm,hr_cand3_snr,hr_cand3_harm_k\n");
+            "hr_cand3_bpm,hr_cand3_snr,hr_cand3_harm_k,"
+            "motion_quality,motion_class,"
+            "rr_candidate_bpm,rr_prominence,rr_axis_support,rr_continuity_quality,rr_harmonic_quality,"
+            "hr_candidate_bpm,hr_prominence,hr_axis_support,hr_continuity_quality,hr_harmonic_quality\n");
 }
 
 static void WriteSecondRow(FILE *out,
@@ -122,7 +125,10 @@
                 "%u,%.6f,%.6f,%.6f,%s,%u,%u,%.9g,%s,%.9g,"
                 "%s,%u,%.6f,%.6f,%u,%.9g,%s,"
                 "%.6f,%.9g,%.6f,%.9g,%u,%u,"
-                "%.6f,%.9g,%u,%.6f,%.9g,%u,%.6f,%.9g,%u\n",
+                "%.6f,%.9g,%u,%.6f,%.9g,%u,%.6f,%.9g,%u,"
+                "%.6f,%s,"
+                "%.6f,%.6f,%.6f,%.6f,%.6f,"
+                "%.6f,%.6f,%.6f,%.6f,%.6f\n",
                 (unsigned int)vital_out->rr_valid,
                 vital_out->rr_bpm,
                 vital_out->rr_raw_bpm,
@@ -148,14 +154,25 @@
                 (unsigned int)vital_out->hr_harmonic_k,
                 vital_out->hr_cand_bpm[0], vital_out->hr_cand_snr[0], (unsigned int)vital_out->hr_cand_harm_k[0],
                 vital_out->hr_cand_bpm[1], vital_out->hr_cand_snr[1], (unsigned int)vital_out->hr_cand_harm_k[1],
-                vital_out->hr_cand_bpm[2], vital_out->hr_cand_snr[2], (unsigned int)vital_out->hr_cand_harm_k[2]);
+                vital_out->hr_cand_bpm[2], vital_out->hr_cand_snr[2], (unsigned int)vital_out->hr_cand_harm_k[2],
+                vital_out->motion_quality,
+                VitalEstimator_MotionClassToString(vital_out->motion_class),
+                vital_out->rr_candidate_bpm,
+                vital_out->rr_prominence,
+                vital_out->rr_axis_support,
+                vital_out->rr_continuity_quality,
+                vital_out->rr_harmonic_quality,
+                vital_out->hr_candidate_bpm,
+                vital_out->hr_prominence,
+                vital_out->hr_axis_support,
+                vital_out->hr_continuity_quality,
+                vital_out->hr_harmonic_quality);
     }
     else
     {
-        /* Fields 27..58: exactly 32 empty vital-result fields.
-         * The preceding formatted section already wrote the comma after field 26.
-         */
-        for (unsigned int i = 0U; i < 31U; i++)
+        /* 43 empty vital-result fields. The preceding section already wrote
+         * the comma after the last non-vital field, so emit 42 more commas. */
+        for (unsigned int i = 0U; i < 42U; i++)
         {
             fputc(',', out);
         }
@@ -233,6 +250,11 @@
             pipeline->reset_since_last_second = 1U;
         }
 
+        if (rest_out.state == REST_STATE_MEASUREMENT)
+        {
+            VitalEstimator_SetMotionContext(&pipeline->vital, &rest_out);
+        }
+
         if ((rest_out.state == REST_STATE_MEASUREMENT) &&
             ((pipeline->second_count % VITAL_ESTIMATE_PERIOD_SEC) == 0U))
         {
